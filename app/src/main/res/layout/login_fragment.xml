<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="#1c7ee4">

    <TextView
        android:layout_width="70dp"
        android:layout_height="30dp"
        android:text="Email"
        android:id="@+id/email_login_textview"
        android:layout_marginBottom="14dp"
        android:layout_above="@+id/password_login_textfield"
        android:layout_alignParentStart="true" />

    <TextView
        android:layout_width="70dp"
        android:layout_height="30dp"
        android:text="Password"
        android:id="@+id/Password_login_textview"
        android:layout_above="@+id/firebase_login_button"
        android:layout_alignParentStart="true" />


    <EditText
        android:id="@+id/email_login_textfield"
        android:layout_width="180dp"
        android:layout_height="wrap_content"
        android:ems="10"
        android:inputType="textEmailAddress"
        android:layout_alignBaseline="@+id/email_login_textview"
        android:layout_alignBottom="@+id/email_login_textview"
        android:layout_toEndOf="@+id/email_login_textview" />

    <EditText
        android:id="@+id/password_login_textfield"
        android:layout_width="180dp"
        android:layout_height="wrap_content"
        android:ems="10"
        android:inputType="textPassword"
        android:layout_centerVertical="true"
        android:layout_toEndOf="@+id/Password_login_textview" />

    <Button
        android:id="@+id/firebase_login_button"
        android:layout_width="100dp"
        android:layout_height="35dp"
        android:text="Login"
        android:layout_below="@+id/password_login_textfield"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="26dp"
        android:textAllCaps="false"
        android:background="@drawable/circlebutton" />
    <com.facebook.login.widget.LoginButton
        android:id="@+id/login_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/firebase_login_button"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="21dp" />

</RelativeLayout>
